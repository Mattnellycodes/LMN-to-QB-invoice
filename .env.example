# QuickBooks OAuth App Credentials (required)
QBO_CLIENT_ID=your_client_id
QBO_CLIENT_SECRET=your_client_secret
QBO_REDIRECT_URI=https://lmn-to-qb-invoice.onrender.com/qbo/callback

# PostgreSQL Database (for production - tokens are stored here automatically)
# On Render, this is set automatically when you link a PostgreSQL database
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Flask secret key (auto-generated on Render, set manually for local dev)
# FLASK_SECRET_KEY=your_secret_key

# Legacy: Token env vars (only needed if NOT using DATABASE_URL)
# These are no longer required - tokens are stored in PostgreSQL on Render
# QBO_ACCESS_TOKEN=...
# QBO_REFRESH_TOKEN=...
# QBO_REALM_ID=...
# QBO_TOKEN_EXPIRES_AT=...
# QBO_REFRESH_EXPIRES_AT=...
